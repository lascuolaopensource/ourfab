# Self-Hosted Supabase (coolify) – Things to add

- make database port publicly available with "${PORT:-5432}:${PORT:-5432}" (check coolify supabase guide)
- do the same with supavisor
  these things must be reachable within the project.

- add GOTRUE_OTP_LENGTH inside docker compose (check for correct var name)

---

# Shelf docker-compose – Things to add

- list all the variables
- add "internal host port"
